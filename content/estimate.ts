import type { ContentSlot } from "@/lib/types";

/**
 * Content slot for the dedicated Get Your Free Estimate page — hosts the
 * primary InquiryForm. Framework phase — no real copy exists yet.
 */
export const ESTIMATE_SLOT: ContentSlot = {
  heading: "Get Your Free Estimate",
};
