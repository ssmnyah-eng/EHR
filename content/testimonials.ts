import type { TestimonialData } from "@/lib/types";

/**
 * No testimonials are populated yet — do not invent quotes or review
 * statistics (brief section 23). Populate this array with real, verified
 * client testimonials during the content/conversion phase.
 */
export const TESTIMONIALS: TestimonialData[] = [];
